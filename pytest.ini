[pytest]
# keep -q if you prefer quieter output; runner shows all logs anyway
addopts = -q --screenshot=off --video=off --tracing=off
markers =
    e2e: end-to-end UI tests (Playwright; slower)
    mock_only: runs only when MOCK_MODE=true
    paper_only: runs only when MOCK_MODE=false
