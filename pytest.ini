[pytest]
addopts = -rA --maxfail=1
markers =
    e2e: end-to-end UI tests
    paper_only: uses live/paper broker keys
base_url = http://127.0.0.1:8501

# If pytest-playwright supports these in your version, keep them here; else omit.
# screenshot = only-on-failure
# video = off
# tracing = off
